# Roblox Studio place/lock files
*.rbxlx
*.rbxl
*.rbxlx.lock
*.rbxl.lock

# Rojo sourcemap
sourcemap.json

# Rojo binaries (large, platform-specific)
tools/
!tools/asset-pipeline/

# Environment variables (API keys)
.env

# Raw assets (textures, audio, meshes, generated — kept local, not in repo)
assets/

# macOS
.DS_Store

# Claude Code local settings
.claude/
